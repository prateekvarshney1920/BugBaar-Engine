{
  "name": "@bugbaar/agents",
  "version": "0.1.0",
  "description": "Agent runtime for BugBaar Engine: LLM providers, memory, goals, and the tool-calling loop.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@bugbaar/tools": "0.1.0"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  }
}
